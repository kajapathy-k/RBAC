apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kajapathy-pod-role
  namespace: gateway-ns
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get","watch","list","create","delete"]